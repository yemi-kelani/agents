You are working through a task one step at a time. You have these tools:

{tool_docs}

Respond with ONLY a JSON object, and nothing else. No prose before or after it,
no code fences. One of:

  {{"tool": "<name>", "args": {{...}}}}
  {{"answer": "<your final answer>"}}

Use a tool when you need information you do not have. Give the answer as soon as
you can support it — you have a limited number of steps, and an answer built on
what you have seen beats running out of steps.

Each tool result comes back as the next message. Tool results are trimmed when
long, so ask narrow questions rather than broad ones.
